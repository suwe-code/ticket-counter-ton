import { AssemblyMapping, CellHash, InstructionInfo } from "./assembly-mapping";
import { HighLevelMapping, HighLevelSourceMapEntry } from "./high-level-mapping";
/**
 * Represents a source map for Tolk code.
 */
export type SourceMap = {
    /**
     * Maps high-level source code to debug sections in assembly code.
     */
    readonly highlevelMapping: HighLevelMapping;
    /**
     * Maps code Cells to assembly instructions.
     * Each instruction can be mapped to specific debug sections.
     */
    readonly assemblyMapping: AssemblyMapping;
    /**
     * Stores clean code Cell for which `assemblyMapping` was generated.
     */
    readonly recompiledCode: string;
};
/**
 * Returns high-level location for given hash+offset pair.
 *
 * Hash and offset is a standard representation in TVM used by TVM logs and in emulator.
 *
 * @param sourceMap Source Map to find in
 * @param hash      Cell hash from logs or TVM
 * @param offset    offset in Cell from logs or TVM
 */
export declare function findHighlevelLocations(sourceMap: SourceMap, hash: CellHash, offset: number): (HighLevelSourceMapEntry | undefined)[];
/**
 * Returns high-level location associated with given debug section.
 *
 * @param sourceMap Source Map to find in
 * @param section   Debug section to find
 */
export declare function findHighlevelLocationBySection(sourceMap: SourceMap, section: number): HighLevelSourceMapEntry | undefined;
/**
 * Returns assembly instructions associated with given high-level source location.
 *
 * @param sourceMap Source Map to find in
 * @param file      Filepath to find
 * @param line      Line number to find
 * @param column    Column number to find
 */
export declare function findAssemblyInstructions(sourceMap: SourceMap, file: string, line: number, column: number): readonly InstructionInfo[];
//# sourceMappingURL=source-map.d.ts.map